const search=document.getElementById('search');const cards=[...document.querySelectorAll('.card')];const buttons=[...document.querySelectorAll('[data-filter]')];const empty=document.getElementById('empty');let filter='todos';function apply(){const q=search.value.toLowerCase().trim();let count=0;cards.forEach(card=>{const category=card.dataset.category;const name=card.dataset.name;const okFilter=filter==='todos'||category===filter;const okSearch=name.includes(q);const show=okFilter&&okSearch;card.style.display=show?'':'none';if(show)count++});empty.hidden=count>0}buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');filter=btn.dataset.filter;apply()}));search.addEventListener('input',apply);apply();